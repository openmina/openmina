#!/bin/sh

LOCAL_MINA_EXE="$(dirname $0)/run_cont_snark_worker.exe"
if [ -x "$LOCAL_MINA_EXE" ]; then
    exec "$LOCAL_MINA_EXE" 3>&1 1>&2
else
    exec docker run --rm --interactive akoptelov/mina-snark-worker-stdio
fi
