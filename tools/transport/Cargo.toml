[package]
name = "mina-transport"
version = "0.9.0"
edition = "2021"

[dependencies]
blake2 = { version = "0.10.6" }
log = { version = "0.4.17" }
hex = { version = "0.4.3" }
pin-project-lite = { version = "0.2.10" }

libp2p = { workspace = true, features = ["tokio", "noise", "pnet", "tcp", "yamux", "dns", "gossipsub"] }
